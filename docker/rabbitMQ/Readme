This script creates a rabbitmq docker image and starts it, exposing ports 15672 and 5672 to the host machine's equivalent ports.

To pull, create and start the image, run the following (from the terminal):
-----------------------------------

** grant the script permissions
$ chmod 700 auto-build

** run the script
$ ./auto-build